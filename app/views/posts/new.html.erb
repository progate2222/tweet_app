<%= form_with(model: @post, local: true) do |form| %>
  <div class="post_content">
    <%= form.label :内容 %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>

  <%= link_to 'Tweet一覧画面へ', posts_path %>
<% end %>
